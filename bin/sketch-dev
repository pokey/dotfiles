#!/usr/bin/env bash
set -euo pipefail

p=$(pwd)
cd ~/src/sketch || exit
go generate 'sketch.dev/loop/...'
envdir ~/envs/bold/anthropic go run ./cmd/sketch -C "$p" -unsafe -skaband-addr=http://localhost:38283 -open "$@"
